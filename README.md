Take the config.def.h from this repository and overwrite the original config.def.h in dwm directory on your system.

Apply the three patches listed in the folder.

Remove config.h and then recompile.
